
<% @categories.each do |cat| %>
    <% if !@category.nil? and cat.title == @category.title %>
        <p class="current_category">
    <% else %>
        <p>
    <% end %>
    <%= link_to cat.title + "(#{cat.listings.count})", category_path(cat), :class => "category" %></p>
<% end %>